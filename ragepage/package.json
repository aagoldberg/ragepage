{
  "name": "ragecheck-extension",
  "version": "1.1.0",
  "description": "RageCheck Chrome Extension - Detect rage bait in social media posts",
  "private": true,
  "scripts": {
    "zip": "cd extension && zip -r ../ragecheck-extension.zip . -x '*.DS_Store'"
  }
}
